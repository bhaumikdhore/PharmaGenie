# ─────────────────────────────────────────────────────────────────────────────
#  PharmaGenie Langfuse Stack — Environment Variables
#  Copy this file to .env and fill in the real values.
# ─────────────────────────────────────────────────────────────────────────────

# ── PostgreSQL (Langfuse internal DB) ────────────────────────────────────────
POSTGRES_USER=langfuse
POSTGRES_PASSWORD=langfuse_secret_CHANGE_ME
POSTGRES_DB=langfuse

# ── Redis ────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=redis_secret_CHANGE_ME

# ── Langfuse auth ────────────────────────────────────────────────────────────
# Must be at least 32 characters
NEXTAUTH_SECRET=change_me_nextauth_secret_must_be_32_chars_long
NEXTAUTH_URL=http://localhost:3100
LANGFUSE_SALT=change_me_unique_salt

# ── Langfuse bootstrap (auto-created on first run) ───────────────────────────
LANGFUSE_INIT_ORG_ID=pharmagenie-org
LANGFUSE_INIT_ORG_NAME=PharmaGenie
LANGFUSE_INIT_PROJECT_ID=pharmagenie-project
LANGFUSE_INIT_PROJECT_NAME=PharmaGenie AI Agents

# These become your SDK keys — change them in production
LANGFUSE_PUBLIC_KEY=pk-lf-pharmagenie-public
LANGFUSE_SECRET_KEY=sk-lf-pharmagenie-secret

# Admin user for the Langfuse UI
LANGFUSE_INIT_USER_EMAIL=admin@pharmagenie.local
LANGFUSE_INIT_USER_NAME=PharmaGenie Admin
LANGFUSE_INIT_USER_PASSWORD=admin123

# ── LLM provider (used by the observability server if it runs its own chains) ─
OPENAI_API_KEY=sk-...your-openai-key...
# ANTHROPIC_API_KEY=
# GROQ_API_KEY=

# ── PharmaGenie backend ───────────────────────────────────────────────────────
# URL the observability server uses to call back to the main backend
PHARMAGENIE_BACKEND_URL=http://localhost:8000
